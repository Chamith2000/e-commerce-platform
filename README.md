# e-commerce-platform
This project builds a microservices-based e-commerce backend with six services: Product, Order, Payment, Cart, Inventory, and Notification. Each runs independently with its own MySQL database. Built using Spring Boot 3.2, it uses an API Gateway (Port 9000) to route requests and hide internal services.
